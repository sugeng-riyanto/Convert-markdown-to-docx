streamlit==1.29.0  # Specify the version of Streamlit you're using
markdown-it-py==3.0.0  # For Markdown parsing
beautifulsoup4==4.12.3  # For HTML parsing
python-docx==1.1.0  # For DOCX file generation
sqlite3==3.43.1  # SQLite database support (usually included in Python, but specify if needed)
